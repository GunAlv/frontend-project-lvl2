const OUTPUT_FORMATS = {
  STYLISH: 'stylish',
};

const FILE_FORMATS = {
  JSON: 'json',
  YAML: 'yaml',
  YML: 'yml',
};

const DIFF_TYPES = {
  ROOT: 'root',
  NESTED: 'nested',
  EQUAL: 'equal',
  REMOVED: 'removed',
  ADDED: 'added',
  UNEQUAL: 'unequal',
};

export { OUTPUT_FORMATS, FILE_FORMATS, DIFF_TYPES };
