const FILE_FORMATS = {
  JSON: 'json',
  YAML: 'yaml',
  YML: 'yml',
};

const DIFF_TYPES = {
  EQUAL: 'equal',
  REMOVED: 'removed',
  ADDED: 'added',
  UNEQUAL: 'unequal',
};

export { FILE_FORMATS, DIFF_TYPES };
